# Native Wan VAE — default external encoder; reproduces the in-DiT VAE path.
# `encoder:` is only consumed when video_backbone.from_scratch=true (else ignored).
# Swap via CLI: model/video_backbone/encoder=dinov3.
name: wan22_vae
model_path: /path/to/Wan2.2-TI2V-5B
