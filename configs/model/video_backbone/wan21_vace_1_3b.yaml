# @package model.video_backbone
# Wan 2.1 VACE-1.3B video DiT.
# DiT geometry: dim=1536, num_heads=12, head_dim=128, num_layers=30.
# Resolution: VACE only supports 480x832 or 720x1280.
# Actual training resolution is determined by the dataloader config.
#
# `from_scratch`: see wan22_ti2v_5b.yaml for semantics.
name: wan21_vace_1_3b
model_path: /path/to/Wan2.1-VACE-1.3B  # [Required] Path to your own directory
from_scratch: false
