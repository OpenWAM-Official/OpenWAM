# @package model.video_backbone
# Wan 2.1 I2V-14B-480P video DiT.
# DiT geometry: dim=5120, num_heads=40, head_dim=128, num_layers=40.
# Resolution: expects 480x832 first-frame conditioning.
# Actual training resolution is determined by the dataloader config.
#
# `from_scratch`: see wan22_ti2v_5b.yaml for semantics.
name: wan21_i2v_14b_480p
model_path: /path/to/weights/Wan2.1-I2V-14B-480P  # [Required] Path to your own directory
from_scratch: false
