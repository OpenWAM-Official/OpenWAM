# Shared Backbone Architecture (experimental stub)
# Action tokens concatenated to video token sequence, processed by same DiT.
# See assets/arch.png (leftmost). The registry keeps this visible for future
# work, but the supported path remains dual_system / moe_expert for now.

architecture:
  type: shared_backbone
  action_dim: 14
  video_dim: 1536
  num_action_tokens: 49
