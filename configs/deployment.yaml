defaults:
  - model: dual_system    # [Optional]: dual_system, moe_expert, shared_backbone
  - _self_

deployment:
  checkpoint_path: null
  device: cuda
  server:
    host: "0.0.0.0"
    ws_port: 8765
    http_port: 8766

project:
  name: open-wam
  seed: 42
