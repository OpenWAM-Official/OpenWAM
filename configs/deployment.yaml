defaults:
  - model: dual_system    # [Optional]: dual_system, moe_expert, shared_backbone
  - _self_

deployment:
  checkpoint_path: null
  device: cuda
  server:
    host: "0.0.0.0"
    ws_port: 8850
    http_port: 8848

project:
  name: open-wam
  seed: 42
