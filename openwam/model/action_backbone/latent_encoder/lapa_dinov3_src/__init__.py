"""LAPA-DINOv3 provider internals: NSVQ model + video/checkpoint helpers.

Not a public surface — :mod:`openwam.model.action_backbone.latent_encoder.lapa_dinov3`
is the entry point. Kept as a sibling ``*_src`` package so the provider file only
holds the contract implementation (mirrors the encoder ``*_src`` layout).
"""
